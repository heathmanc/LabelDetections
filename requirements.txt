# label_detections/core is stdlib-only on purpose: the label schema, the review
# gate and the dataset split are all unit testable with nothing installed.
# Everything below is for the UI, the cameras and the training stack.

PySide6>=6.6
opencv-python>=4.8
numpy>=1.24
ultralytics>=8.3.0
PyYAML>=6.0
pypylon>=3.0

# Code reading. zxing-cpp covers 1D, QR, DataMatrix, Aztec and PDF417 in one
# library; pylibdmtx is the fallback for beaten-up DataMatrix it refuses.
zxing-cpp>=2.2

# Reading the printed part number, for labels whose code the camera cannot
# resolve -- on a view wide enough to take in a whole battery, a barcode's
# narrowest bar lands below what any decoder recovers while the text beside it
# is still comfortable. Self-contained wheels with the models bundled, and no
# system binary to install alongside.
rapidocr-onnxruntime>=1.3
pylibdmtx>=0.1.10
